{% if combat %}
<add-to-battle
  name="{{ creatureName or title }}"
  kind="{% if 'character' in tags %}player{% else %}monster{% endif %}"
  ac="{{ combat['КБ'].value }}"
  hp="{{ combat['КЗ'].value }}"
  init-mod="{{ combat['Инициатива'].value }}"
  image="{% if preview %}{{ page.url | url }}{{ preview }}{% elif portrait %}{{ page.url | url }}{{ portrait }}{% elif gallery %}{{ page.url | url }}{{ gallery | first }}{% endif %}"
  size="{{ size or 'medium' }}"
  link="{{ page.url | url }}"
  label-added="Добавлено">В бой</add-to-battle>
{% endif %}
