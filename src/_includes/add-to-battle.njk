{% if combat %}
<add-to-battle
  name="{{ creatureName or title }}"
  kind="{% if 'character' in tags %}player{% else %}monster{% endif %}"
  ac="{{ combat['КБ'].value }}"
  hp="{{ combat['КЗ'].value }}"
  init-mod="{{ combat['Инициатива'].value }}"
  image="{% if previewImage %}{{ page.url | url }}{{ previewImage }}{% endif %}"
  size="{{ size or 'medium' }}"
  link="{{ page.url | url }}"
  label-added="Добавлено">В бой</add-to-battle>
{% endif %}
